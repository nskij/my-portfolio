import React, { Component } from 'react';
import '../App.css';

export default class Slill extends Component {
  render() {
    return(
      <div>
        <p>使える技術についてです。</p>
      </div>
    );
  }
}